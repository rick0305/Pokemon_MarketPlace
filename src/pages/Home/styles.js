import styled from "styled-components";

//Estilização
export const Styled = {
    CardWrapper: styled.div`
        display: grid;
        margin: 0 auto;
        width: 70%;
        grid-template-columns: repeat(4, auto);
    `

}